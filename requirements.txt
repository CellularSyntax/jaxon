# Pip-only fallback (use if not on conda). For CUDA: replace
#   jax[cpu]==0.6.2
# with
#   jax[cuda12]==0.6.2
# and reinstall.

jax[cpu]==0.6.2
jaxley==0.13.0
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
pandas==2.3.3
optax==0.2.8
tqdm
neuron==9.0.1
pyfibers==0.8.5
pytest
ipykernel
