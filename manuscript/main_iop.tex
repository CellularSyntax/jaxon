% IOP / JNE submission variant of main.tex.
%
% This wrapper file simply enables the \jneSubmission switch (which
% main.tex uses to select iopjournal.cls instead of article class) and
% then \input's the full manuscript.  Everything else lives in main.tex
% so there is only one copy of the manuscript content to maintain.
%
% Build:
%     make iop                  (preferred)
%     latexmk -pdf main_iop.tex (manual)
%
% Output: main_iop.pdf
\def\jneSubmission{1}
\input{main}
