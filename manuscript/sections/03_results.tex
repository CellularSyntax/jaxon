\section{Results}
\label{sec:results}

We benchmarked jaxon across four axes: \emph{(i)} numerical agreement
with NEURON on activation thresholds, conduction velocity and
intracellular traces; \emph{(ii)} reproduction of canonical
propagation phenomena (AP collision, kHz and DC conduction block,
repetitive-stimulation desynchronisation); \emph{(iii)} forward-pass
throughput against the standard \texttt{pyfibers} pipeline on a single
GPU; and \emph{(iv)} differentiable selectivity optimisation on
randomised multi-fascicle nerves.  All raw data and reproduction
scripts are in the repository under
\texttt{outputs/} and \texttt{experiments\_v2/}.

\subsection{Validation against NEURON}
\label{sec:results-validation}

For each axon model we drove jaxon and \texttt{pyfibers}-wrapped
NEURON with the identical stimulus and compared the activation
threshold (the lowest amplitude at which the centre node fired an
AP) across diameters, pulse widths and stimulus waveforms.  In
total we ran 943 distinct test configurations, spanning all four
models and the diameters they are parameterised over, eight stimulus
waveforms (monophasic cathodic / anodic, biphasic cathodic-first /
anodic-first, sinusoidal, sawtooth, exponentially-decaying and
Gaussian), and six pulse widths ($0.02$--$1\,\mathrm{ms}$).
\textbf{$99.6\,\%$ of the 943 configurations agree to within $1\,\%$
relative threshold error with NEURON}
(Table~\ref{tab:validation-sd}).  Across the same 943 configurations,
the Pearson correlation between jaxon and NEURON thresholds is
$r = 0.999998$ ($R^{2} = 0.999996$), with a mean signed bias of
$+0.11\,\%$ (jaxon marginally high) and $95\,\%$ limits of agreement
of $[-0.05\,\%, +0.41\,\%]$.  Only the Rattay model has
configurations exceeding the $1\,\%$ acceptance criterion, and those
exceptions are concentrated in a single edge regime
(biphasic cathodic-first pulses at the shortest pulse width
$\mathrm{PW}=0.1\,\mathrm{ms}$ across the four smallest Rattay
diameters; peak $2.24\,\%$ at $D=0.3\,\mu\mathrm{m}$).  We attribute
this to the bisection-based threshold detector having reduced
resolution near the inflection point of the biphasic cathodic-first
strength--duration curve at the smallest diameters; the
intracellular voltage traces themselves agree to NEURON-level
precision (Figure~\ref{fig:traces}, bottom rows).

\begin{table}[h]
  \centering
  \caption{Strength--duration threshold agreement of jaxon vs.\
    \texttt{pyfibers}-wrapped NEURON, aggregated across all tested
    diameters, waveforms and pulse widths per model.
    Diameters tested ($\mu$m):
    MRG = \{5.7, 7.3, 8.7, 10.0, 11.5, 12.8, 14.0, 15.0, 16.0\};
    Sundt = \{0.3, 0.5, 0.8, 1.0, 1.2\};
    Sweeney = \{10\};
    Rattay = \{0.3, 0.5, 0.8, 1.0, 1.5\}.
    Sweeney is evaluated at $D=10\,\mu\mathrm{m}$ only because Sweeney's
    original parameterisation~\cite{sweeney1987} was derived and
    validated at $D=10\,\mu\mathrm{m}$ and the pyfibers reference
    implementation~\cite{hussain2024} follows the same convention;
    the 17 cells dropped from Sweeney's 48-cell sweep at $D=10$ are
    NEURON \texttt{extracellular}-mechanism singular-solve failures
    under anodic-leading pulses, not jaxon errors (the jaxon values
    in those cells are themselves finite and consistent with the
    surrounding cathodic results).
    Waveforms tested: monophasic cathodic and anodic, biphasic
    cathodic-first and anodic-first, sinusoidal, sawtooth,
    exponentially-decaying, Gaussian (8 total).  Pulse widths tested:
    \{0.02, 0.05, 0.1, 0.2, 0.5, 1.0\}\,ms (6 total).}
  \label{tab:validation-sd}
  \begin{tabular}{lrcrr}
    \toprule
    Model    & configs tested & median err.\ [IQR] & 95\textsuperscript{th} \%ile & peak err.\ \\
    \midrule
    MRG      &  432 & 0.04\,\% [0.02--0.09] & 0.20\,\% & 0.39\,\% \\
    Sundt    &  240 & 0.34\,\% [0.28--0.39] & 0.42\,\% & 0.46\,\% \\
    Sweeney  &   31 & 0.02\,\% [0.02--0.05] & 0.06\,\% & 0.06\,\% \\
    Rattay   &  240 & 0.07\,\% [0.04--0.10] & 0.15\,\% & 2.24\,\% \\
    \midrule
    \textbf{total} & \textbf{943} & \textbf{0.08\,\% [0.03--0.20]} & \textbf{0.40\,\%} & \textbf{2.24\,\%} \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:validation-sd} aggregates across all eight stimulus
waveforms.  Because waveform shape couples nonlinearly into the
strength--duration relationship and into the activation
proxy~\cite{grill1995stimulus,wongsarnpigoon2010energy}, we also
report the breakdown per waveform aggregated across all four fiber
models (Table~\ref{tab:validation-sd-per-wf}).  Seven of the eight
waveforms agree to within $1\,\%$ on \emph{every} configuration
tested; the only exceptions are four biphasic cathodic-first
configurations of the smallest Rattay diameters (already discussed
above), and even there 96.5\,\% of the 115 biphasic-cath-first
configurations remain below the $1\,\%$ tolerance.  The visual
analogue is Figure~\ref{fig:validation-waveforms}, which overlays
the strength--duration curves for all eight waveforms at a single
representative diameter ($D=10\,\mu\mathrm{m}$ MRG) and reports the
distribution of relative threshold errors per waveform aggregated
across the full per-model sweep.

\begin{table}[h]
  \centering
  \caption{Threshold error breakdown per stimulus waveform,
    aggregated across all four fiber models and their published
    diameter ranges (8 waveforms $\times$ ${\sim}120$ configurations
    each).  All eight waveform shapes used in the sweep are reported
    --- monophasic cathodic, monophasic anodic, biphasic
    cathodic-first, biphasic anodic-first, sinusoidal, sawtooth,
    exponentially-decaying and Gaussian (cf.\ Section~\ref{sec:methods-validation}).
    Apart from a handful of biphasic cathodic-first configurations at
    the four smallest Rattay diameters, every other waveform passes
    the $1\,\%$ tolerance on every diameter tested.}
  \label{tab:validation-sd-per-wf}
  \begin{tabular}{lrccrr}
    \toprule
    Waveform & $n$ & median [IQR] err. & 95\textsuperscript{th} \%ile & peak err. & \%\,$<1\%$ \\
    \midrule
    Monophasic cathodic       & 120 & 0.08\,\% [0.04--0.23] & 0.40\,\% & 0.44\,\% & 100.0\,\% \\
    Monophasic anodic         & 114 & 0.12\,\% [0.05--0.21] & 0.38\,\% & 0.41\,\% & 100.0\,\% \\
    Biphasic cathodic-first   & 115 & 0.08\,\% [0.03--0.19] & 0.38\,\% & 2.24\,\% &  96.5\,\% \\
    Biphasic anodic-first     & 114 & 0.08\,\% [0.03--0.17] & 0.39\,\% & 0.42\,\% & 100.0\,\% \\
    Sinusoidal                & 120 & 0.05\,\% [0.03--0.23] & 0.39\,\% & 0.43\,\% & 100.0\,\% \\
    Sawtooth                  & 120 & 0.07\,\% [0.04--0.27] & 0.41\,\% & 0.46\,\% & 100.0\,\% \\
    Exponential decay         & 120 & 0.07\,\% [0.03--0.23] & 0.41\,\% & 0.44\,\% & 100.0\,\% \\
    Gaussian                  & 120 & 0.05\,\% [0.03--0.25] & 0.37\,\% & 0.42\,\% & 100.0\,\% \\
    \midrule
    \textbf{total}           & \textbf{943} & \textbf{0.08\,\% [0.03--0.20]} & \textbf{0.40\,\%} & \textbf{2.24\,\%} & \textbf{99.6\,\%} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_validation_waveforms.png}
  \caption{Per-waveform validation against \texttt{pyfibers}-wrapped
    NEURON.  \textbf{Top row:} strength--duration threshold (absolute
    value) vs.\ pulse width for each of the eight stimulus waveforms,
    at one representative diameter ($D=10\,\mu\mathrm{m}$ MRG): jaxon
    (solid coloured line) overlays NEURON (open circles) at every
    tested pulse width for every waveform.  \textbf{Bottom row:}
    distribution of $|$relative threshold error$|$ for the same
    waveform, aggregated over all four fiber models and their
    diameter sweeps ($n\approx 120$ configurations per panel; vertical
    dashed line = per-waveform median, vertical dotted red line =
    $1\,\%$ acceptance criterion).  X-axes are shared (0--2.5\,\%) so
    the panels are directly visually comparable: seven of the eight
    waveforms fall entirely below the $1\,\%$ tolerance, only the
    biphasic cathodic-first panel has a tail above $1\,\%$ (the four
    smallest-Rattay outliers identified in Table~\ref{tab:validation-sd}).}
  \label{fig:validation-waveforms}
\end{figure}

Conduction velocity (CV) is independently sensitive to the
internodal and nodal time constants and so probes a different
component of the model.  Table~\ref{tab:validation-cv} reports the
CV agreement across the published diameter range of each model.
For MRG, Sweeney and Rattay the median error rounds to zero across
every tested diameter; the Sundt model shows a peak $0.24\,\%$
error at the smallest tested diameter ($D=0.3\,\mu\mathrm{m}$).
For Rattay, the AP peak voltage at the centre node sits near
$-4\,\mathrm{mV}$ (well below the $0\,\mathrm{mV}$ crossing
threshold conventionally used elsewhere in the suite), so CV
extraction uses a $-30\,\mathrm{mV}$ crossing threshold instead;
under this convention the CV agreement is sub-percent across the
full diameter range.

\begin{table}[h]
  \centering
  \caption{Conduction velocity agreement against NEURON, evaluated
    at each tested diameter for each model.  Diameter sets follow
    the parameterisation range published with each model.  Sweeney
    is restricted to $D=10\,\mu\mathrm{m}$ by Sweeney 1987
    convention (see Table~\ref{tab:validation-sd} caption).}
  \label{tab:validation-cv}
  \small
  \begin{tabular}{l p{5.4cm} cr}
    \toprule
    Model    & diameters tested ($\mu$m) & median err.\ [IQR] & peak err.\ \\
    \midrule
    MRG      & 5.7, 7.3, 8.7, 10.0, 11.5, 12.8, 14.0, 15.0, 16.0  & $<0.01$\,\% [0.00--0.00]  & $<0.01$\,\% \\
    Sundt    & 0.3, 0.5, 0.8, 1.0, 1.2                             & $0.09$\,\% [0.08--0.16]   & $0.24$\,\%  \\
    Sweeney  & 10                                                  & $<0.01$\,\% [0.00--0.00]  & $<0.01$\,\% \\
    Rattay   & 0.3, 0.5, 0.8, 1.0, 1.5                             & $0.05$\,\% [0.02--0.05]   & $0.13$\,\%  \\
    \bottomrule
  \end{tabular}
\end{table}

Figure~\ref{fig:validation} compactly summarises the three validation
axes for all four models: the strength--duration curve at the nominal
diameter (top row, jaxon as solid line, NEURON as open circles),
CV-vs.-diameter agreement (middle row), and the empirical distribution
of relative threshold error across the full sweep (bottom row, with
median and the $1\,\%$ acceptance criterion shown).  Every histogram
is wholly to the left of the red $1\,\%$ line.  Because the manuscript
ultimately targets \emph{extracellular} stimulation (Section
\ref{sec:results-selectivity}), we additionally verified that the
full extracellular-stimulation membrane-potential response at the
centre node matches NEURON's analytic update within the
$\Delta t$-quantisation noise floor of the comparison
(Figure~\ref{fig:traces}, top two rows); intracellular
current-injection responses are also reported on the same fibres
(Figure~\ref{fig:traces}, bottom two rows) and show comparable
agreement.

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_validation_4models.png}
  \caption{Validation of jaxon against \texttt{pyfibers}-wrapped NEURON
    across all four fiber models.  \textbf{Top:} strength--duration
    threshold (absolute value) vs.\ pulse width for monophasic cathodic
    pulses at the nominal diameter ($D = 10\,\mu\mathrm{m}$ for MRG,
    Sweeney and Rattay; $D = 1\,\mu\mathrm{m}$ for Sundt).  jaxon (solid,
    coloured) overlies NEURON (open circles) at every pulse width.
    \textbf{Middle:} conduction velocity (CV) vs.\ fiber diameter across
    the manufacturer-supported range for each model.  \textbf{Bottom:}
    empirical distribution of relative threshold error across the full
    sweep of waveforms, pulse widths and diameters for that model
    ($n$ = 432 MRG, 240 each for Sundt, Sweeney, Rattay).  Median is
    marked with a dashed black line; the $1\,\%$ acceptance criterion
    is the red dotted line.}
  \label{fig:validation}
\end{figure}

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_traces_4models.png}
  \caption{Membrane-potential response to a single extracellular
    cathodic stimulus (top two rows) and intracellular current
    injection (bottom two rows), jaxon vs.\ NEURON, for all four
    fibre models.
    \textbf{Row~1 (extracellular stim):} the applied extracellular
    cathodic rectangular pulse $I_e(t)$ delivered at $t = 1\,\mathrm{ms}$
    (PW $= 0.1\,\mathrm{ms}$ for MRG and Sweeney; $0.2\,\mathrm{ms}$
    for Sundt and Rattay).  Grey dotted line: model-specific
    activation threshold.  Annotated: suprathreshold amplitude actually
    applied and threshold.
    \textbf{Row~2 (extracellular $V_m$):} membrane potential at the
    centre node, jaxon (coloured solid) over NEURON (dashed black).
    Peak absolute voltage discrepancy $|\Delta V_m^{\mathrm{peak}}|$
    annotated; in every case below the $\Delta t = 5\,\mu\mathrm{s}$
    quantisation noise floor.
    \textbf{Row~3 (intracellular stim):} the applied intracellular
    current pulse $I_i(t)$ (1.0\,nA MRG, 3.0\,nA Sweeney,
    0.5\,nA Sundt / Rattay; same delay and pulse-width convention).
    \textbf{Row~4 (intracellular $V_m$):} membrane response, same
    overlay convention.  The two stimulation regimes test the
    integrator independently — the extracellular case is the one
    that matters for selectivity optimisation
    (Section~\ref{sec:results-selectivity}).}
  \label{fig:traces}
\end{figure}

\subsection{Propagation phenomena}
\label{sec:results-phenomena}

Beyond threshold agreement, we verified that jaxon reproduces four
characteristic propagation phenomena that arise from the nonlinear
coupling of the axon's active membrane to the extracellular field.
Each phenomenon was run independently in jaxon and in
\texttt{pyfibers}-wrapped NEURON with identical parameters;
Figure~\ref{fig:phenomena} overlays the two as solid lines (jaxon) and
dashed lines (NEURON).

\paragraph{AP collision.}
Two cathodic pulses delivered simultaneously from opposite ends of a
$5.7$--$14\,\mu\mathrm{m}$ MRG fiber initiate two action potentials
that propagate inward, meet near the centre, and annihilate due to
mutual refractory-period overlap (Figure~\ref{fig:phenomena}\textbf{a}).
The peak voltage discrepancy between jaxon and NEURON at the moment
of collision is below $1\,\mu\mathrm{V}$ for every tested diameter, an
agreement bounded by the floating-point precision of the comparison
itself.

\paragraph{kHz conduction block.}
A continuous $20\,\mathrm{kHz}$ sinusoidal extracellular hold of
sufficient amplitude prevents AP propagation past the source
node~\cite{kilgore2014block}.  We delivered a train of fourteen
cathodic pace pulses ($1.5\,\mathrm{nA}$, $0.1\,\mathrm{ms}$
pulse width, $10\,\mathrm{ms}$ intervals) and varied the kHz amplitude;
Figure~\ref{fig:phenomena}\textbf{b} shows the distal-node voltage
trace for four amplitudes.  The block threshold (the smallest
amplitude at which no APs propagate past the source) computed by
jaxon matches NEURON within the
$0.01\,\mathrm{mA}$ scan resolution; the AP count at each amplitude
agrees exactly.

\paragraph{DC conduction block.}
A constant hyperpolarising extracellular DC source positioned $1\,\mathrm{mm}$
from the fiber inhibits AP propagation when its amplitude exceeds a
diameter-dependent threshold.  Figure~\ref{fig:phenomena}\textbf{c}
shows the distal-node trace for four DC amplitudes scaled around the
jaxon-computed block threshold.  jaxon reproduces NEURON's
sub-threshold pass-through and supra-threshold block crisply, with
threshold agreement under $1\,\%$.

\paragraph{Spike desynchronisation by repetitive stimulation.}
A sequence of three closely-spaced suprathreshold cathodic pulses
followed by a perturbation pulse of variable amplitude tests the
fiber's ability to lock to the perturbation rate.
Figure~\ref{fig:phenomena}\textbf{d} reports the sync index (the
fraction of perturbation cycles that elicit an AP) vs.\ perturbation
amplitude at three inter-stimulus frequencies (30, 50 and 100\,Hz).
The sync curves predicted by jaxon and NEURON coincide to within the
spike-detection quantisation resolution.

\begin{figure}[h]
  \centering
  \figmaybe[width=0.95\textwidth]{fig_phenomena.png}
  \caption{Reproduction of four canonical propagation phenomena under
    jaxon (solid coloured lines) vs.\ NEURON (dashed black).
    \textbf{(a)} Action-potential collision in a $10\,\mu\mathrm{m}$ MRG
    fiber driven from both ends; snapshots of $V_m$ along the fiber at
    five times show the two APs meeting and annihilating.
    \textbf{(b)} kHz conduction block at $20\,\mathrm{kHz}$ extracellular
    hold; distal-node $V_m$ traces show 0, partial and full blocking
    of the 14-pulse pace train as the block amplitude increases.
    \textbf{(c)} DC conduction block via hyperpolarising extracellular
    source; distal-node $V_m$ vs.\ block amplitude factor relative to
    the jaxon-computed block threshold.
    \textbf{(d)} Sync index of perturbation responses for
    repetitive-stimulation pacing at three inter-stimulus frequencies
    (30, 50, 100\,Hz); curves rise from below-threshold (sync = 0) to
    one-spike-per-pulse (sync = 1) over the same amplitude range in
    jaxon and NEURON.}
  \label{fig:phenomena}
\end{figure}

\subsection{Forward-pass throughput}
\label{sec:results-scaling}

The principal performance motivation for the JAX implementation is
the expectation that GPU vmap-batching will outperform CPU-parallel
NEURON as the fiber count grows.  We benchmarked all five
implemented axon models (MRG with both the original and the
interpolated parameter tables, Sweeney, Sundt and Rattay) at
$N \in \{1, 10, 100, 1\,000, 10\,000, 100\,000\}$ fibers on a single
NVIDIA A100 GPU, against \texttt{pyfibers} running 8-thread
CPU-parallel on a matched Xeon host.
Figure~\ref{fig:scaling} reports the per-model wall time (panel A)
and the resulting jaxon-vs-pyfibers speedup (panel B);
Table~\ref{tab:scaling} summarises the headline numbers.

At small $N$, GPU launch overhead dominates and \texttt{pyfibers}
on CPU is faster (jaxon takes $0.15$\,s vs.\ \texttt{pyfibers}'s
$0.05$\,s on $N=1$ MRG).  Throughput then scales sub-linearly with
$N$ on the GPU because the vmap-batched forward pass amortises the
per-step launch cost across all fibers in the batch; the crossover
at which jaxon overtakes \texttt{pyfibers} is $N \approx 10$ for every
model tested.  By $N=1\,000$ MRG, jaxon completes in $0.24\,\mathrm{s}$
vs.\ \texttt{pyfibers}'s $49.3\,\mathrm{s}$ (a $209\times$ wall-time
reduction); by $N=10\,000$ MRG, $0.78\,\mathrm{s}$ vs.\
$525\,\mathrm{s}$ ($671\times$); by $N=100\,000$ MRG,
$9.5\,\mathrm{s}$ vs.\ $5\,172\,\mathrm{s}$ ($544\times$).  Sweeney,
whose smaller per-fiber state vector fits more efficiently in GPU
registers, achieves the largest single-model speedup at
$N=100\,000$: $4.3\,\mathrm{s}$ vs.\ $5\,972\,\mathrm{s}$,
a factor of $1\,377\times$.  The unmyelinated Sundt and Rattay
C-fibres, whose longer time courses make \texttt{pyfibers}
substantially slower per fiber, reach $1\,006\times$ and $929\times$
speedups respectively at $N=100\,000$ ($16.5$ vs.\
$16\,593\,\mathrm{s}$ and $15.1$ vs.\ $14\,068\,\mathrm{s}$).
The geometric mean across all five models at $N=100\,000$ is
$\sim 820\times$, taking what would have been multiple CPU-hours of
\texttt{pyfibers} time per forward pass down to under twenty
seconds of GPU wall time.
For the fascicular selectivity sweeps reported in
Section~\ref{sec:results-selectivity}, where $\sim 1\,000$ fibers per
nerve realisation are simulated thousands of times during
optimisation, this throughput differential is what brings
multi-anatomy gradient-based optimisation on a 12-contact cuff into
reach on a single GPU-day.

\begin{table}[h]
  \centering
  \caption{Wall time per forward simulation (in seconds) and speedup
    of jaxon over \texttt{pyfibers} at representative fiber counts.
    jaxon runs on a single NVIDIA A100 GPU; \texttt{pyfibers} runs
    8-thread CPU-parallel on a matched Xeon host.  All timings exclude
    JAX compile time (5--30\,s one-time per model, amortised over each
    batched run).  Geometric mean of the $N=100\,000$ speedups across
    the five benchmarked models is $\sim 820\times$.}
  \label{tab:scaling}
  \begin{tabular}{lrrrrrr}
    \toprule
                            & \multicolumn{2}{c}{$N=1\,000$}
                            & \multicolumn{2}{c}{$N=10\,000$}
                            & \multicolumn{2}{c}{$N=100\,000$} \\
    \cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
    Model      & pyf.\ (s) & speedup & pyf.\ (s) & speedup & pyf.\ (s) & speedup \\
    \midrule
    MRG        &  49.3 & $209\times$ &     525 & $671\times$ &  $5\,172$ &  $544\times$ \\
    MRG-interp.&  58.0 & $246\times$ &     537 & $695\times$ &  $5\,021$ &  $527\times$ \\
    Sweeney    &  62.6 & $268\times$ &     603 & $1\,322\times$ & $5\,972$ & $1\,377\times$ \\
    Sundt      &   183 & $304\times$ & $1\,742$ &   $985\times$ & $16\,593$ & $1\,006\times$ \\
    Rattay     &   151 & $259\times$ & $1\,425$ &   $868\times$ & $14\,068$ &   $929\times$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_scaling_allmodels.png}
  \caption{Forward-pass throughput on a single NVIDIA A100 GPU as a
    function of fiber count $N$ for all five benchmarked axon models
    (MRG, MRG with interpolated parameter tables, Sweeney, Sundt and
    Rattay).
    \textbf{(a)} Absolute wall time per forward simulation.  Dashed
    coloured lines + open markers: \texttt{pyfibers} on 8-thread CPU.
    Solid coloured lines: jaxon on a single A100.  jaxon's curves are
    nearly flat up to $N \approx 100$ as the GPU is under-utilised,
    then scale linearly.  \texttt{pyfibers} scales linearly throughout.
    \textbf{(b)} Implied speedup; horizontal grey reference at
    $1\times$.  Crossover at $N \approx 10$ for every model; at
    $N = 100\,000$ all five exceed $500\times$ speedup, with Sweeney
    reaching $1\,377\times$ and the unmyelinated Sundt / Rattay
    C-fibres exceeding $900\times$.}
  \label{fig:scaling}
\end{figure}

\subsection{Selectivity optimisation on multi-fascicle nerves}
\label{sec:results-selectivity}

\subsubsection*{Cross-model single-diameter sweep.}
We applied the rectangular-pulse and warm-started waveform optimisers
of Section~\ref{sec:methods-opt} to randomised Hussain-style
multi-fascicle nerve realisations
(Section~\ref{sec:methods-geom}) for all four implemented fibre
models, at the representative diameters used in the
benchmark / prior literature: MRG at $D=5.7\,\mu\mathrm{m}$ and
$D=10\,\mu\mathrm{m}$, Sweeney at $D=10\,\mu\mathrm{m}$, Sundt at
$D=1\,\mu\mathrm{m}$, and Rattay at $D=1\,\mu\mathrm{m}$.  Per fibre
model we ran 25 randomised nerve realisations; for each realisation
we ran the multi-start LBFGS rectangular optimiser (Adam-FD variant
in this sweep) to a budget of up to 90 inner Adam iterations
(early-stopping once $\mathrm{SI}\ge 1$ or the loss plateaus for 20
iterations), followed by up to 100 iterations of the warm-started
waveform Adam optimiser (learning rate $5\times 10^{-4}$,
early-stopping patience 20).

A critical design detail is the random target / off-target divider:
on every seed the line that splits fascicles into the target group
and the off-target group is drawn uniformly in $[0°,\,180°)$, so the
sweep covers every possible cardiac-vs-non-cardiac fascicle
orientation rather than baking in a single (e.g.\ dorsal-vs-ventral)
configuration.  This randomisation, together with the
rejection-sampled fascicle positions, gives 25 distinct
geometric / topological problems per fibre model.

Across the 125 randomised realisations (5 models $\times$ 25 seeds)
the rectangular optimiser achieves
\textbf{median $\mathrm{SI}=1.000$ on every model}, with cross-model
mean SI from $0.934$ (Sundt) to $0.979$ (Rattay), and between
$64\,\%$ and $76\,\%$ of seeds reaching the theoretical maximum
$\mathrm{SI}=1.000$ (all target fascicle fibres firing, all
off-target fibres silent) per model (Table~\ref{tab:selectivity}).
The warm-started waveform stage preserves the rectangular optimum on
the majority of seeds; on a small subset ($\sim 25$--$35\,\%$
depending on model) the wave Adam step destabilises the warm-start
slightly, and the wave-stage report defaults to the best-iter SI
which is at minimum the warm-start (rect) value.
Figure~\ref{fig:selectivity}\textbf{a} shows the per-model SI
distribution (one paired rect+wave violin per model);
Figure~\ref{fig:selectivity}\textbf{b} the per-seed wave-vs-rect
comparison across all 125 seeds.

The model with the lowest mean SI is Sundt (mean $0.934$,
$68\,\%\ge 0.95$).  Sundt's lower mean reflects two compounding
factors: its peak AP voltage at the centre node is the lowest of
the four models ($\sim 11\,\mathrm{mV}$, vs $\sim 23\,\mathrm{mV}$ for
MRG), so the activation proxy operates closer to its sigmoid
saturation; and its activation threshold at the 1.5\,mm cuff is the
highest ($\sim 50\,\mathrm{mA}$, requiring per-model amp scaling of
the rect optimiser as detailed in
Section~\ref{sec:methods-opt}).  Even so, Sundt reaches
$\mathrm{SI}\ge 0.95$ on the majority of seeds, and Rattay --- the
other unmyelinated model --- achieves the \emph{highest} mean SI of
the four, demonstrating that the cuff-design pipeline generalises
across fibre biophysics.

\begin{table}[h]
  \centering
  \caption{Per-model selectivity statistics across 25 randomised
    Hussain-style nerves per model.  Each seed uses a uniformly random
    divider angle in $[0°,\,180°)$ and a freshly reject-sampled
    fascicle packing.  Median [IQR] reported alongside the mean to
    show distribution spread; the rect median is pinned at $1.000$ on
    every model, but the Q1 of the IQR captures how tightly the bulk
    of seeds clusters at the optimum.  Wave columns are the
    warm-started waveform-Adam optimum.}
  \label{tab:selectivity}
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{lrrcrrrc}
    \toprule
    Model           & D ($\mu$m) & rect mean & rect median [IQR] & \%\,$\ge 0.95$ & \%\,$=1.0$ & wave mean & wave median [IQR] \\
    \midrule
    MRG (myel.)     &  5.7 & 0.962 & 1.000 [0.97--1.00] & 84\,\% & 72\,\% & 0.952 & 0.990 [0.96--1.00] \\
    MRG (myel.)     & 10.0 & 0.944 & 1.000 [0.95--1.00] & 76\,\% & 68\,\% & 0.941 & 1.000 [0.93--1.00] \\
    Sweeney (myel.) & 10.0 & 0.956 & 1.000 [1.00--1.00] & 80\,\% & 76\,\% & 0.950 & 0.990 [0.96--1.00] \\
    Sundt (unmyel.) &  1.0 & 0.934 & 1.000 [0.94--1.00] & 68\,\% & 64\,\% & 0.928 & 0.990 [0.94--1.00] \\
    Rattay (unmyel.) & 1.0 & 0.979 & 1.000 [0.99--1.00] & 88\,\% & 72\,\% & 0.958 & 0.987 [0.94--1.00] \\
    \midrule
    \textbf{all 125 seeds} & --- & \textbf{0.955} & \textbf{1.000 [0.96--1.00]} & \textbf{79\,\%} & \textbf{70\,\%} & \textbf{0.946} & \textbf{0.990 [0.95--1.00]} \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

Figure~\ref{fig:selectivity-gallery} shows representative
cross-sections per model and per seed: the random divider angle and
the rejection-sampled fascicle positions give each panel a different
target / off-target layout, and the optimised cuff configuration
(per-contact polarities + amplitudes) adapts to it.
Figure~\ref{fig:convergence} reports the per-model convergence
profiles across the 125 seeds: the rect stage drives the WQ loss down
by roughly an order of magnitude over $\sim 20$--$60$ iterations
(panel a, with model-dependent rates --- MRG and Sweeney converge
fastest, the unmyelinated Sundt and Rattay take more iterations),
while the warm-started waveform stage holds the rect optimum's SI
near 1.0 throughout (panel b, median always $\ge 0.95$ across all
five model / diameter combinations).

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_optimization_convergence.png}
  \caption{Per-model optimisation convergence across the 125
    randomised Hussain-style nerve realisations.  In each panel the
    solid line is the per-iteration median across the seeds for that
    fibre model, the shaded band is the inter-quartile range
    (Q1--Q3), and seeds that stop early are padded with their final
    value so the curve is well-defined out to the longest run.
    \textbf{(a)} Rectangular LBFGS-FD stage --- WQ loss vs.\ optimiser
    iteration.  All models drop their loss by roughly an order of
    magnitude over the first $\sim 30$ iterations; MRG and Sweeney
    converge fastest, Sundt and Rattay (unmyelinated, higher
    activation thresholds) take more iterations.
    \textbf{(b)} Warm-started waveform-Adam stage --- selectivity
    index vs.\ iteration.  Wave Adam starts from the rect optimum and
    mostly preserves it; the median SI stays above the $0.95$
    acceptance line (red dashed) throughout for every model.  The
    legend ``$n=N$'' is the number of seeds whose wave history has
    length $>1$ (seeds that already satisfied the
    $\mathrm{SI}\ge 1$ early stop at warm-start contribute one point
    only and are excluded from the per-iteration aggregation).}
  \label{fig:convergence}
\end{figure}

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_selectivity_summary.png}
  \caption{Selectivity optimisation across 125 randomised
    Hussain-style multi-fascicle nerve realisations spanning 5 fibre
    model / diameter combinations (25 seeds each).
    \textbf{(a)} Per-model paired-violin distributions: rectangular
    LBFGS-FD (left of each pair) and warm-started waveform-Adam
    (right of each pair).  Individual seeds shown as jittered points;
    red bars mark per-stage medians; red dashed line marks the
    $\mathrm{SI}=0.95$ acceptance criterion used in the prior
    literature.
    \textbf{(b)} Per-seed scatter of warm-started waveform SI vs.\
    rectangular SI, colour-coded by model.  Points on the dotted
    diagonal are seeds where the waveform stage preserved the rect
    optimum exactly; points above the diagonal are improvements,
    below are regressions.}
  \label{fig:selectivity}
\end{figure}

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_selectivity_xsections.png}
  \caption{Cross-section activation patterns at the rectangular LBFGS
    optimum.  Two seeds per fibre model (rows: MRG~5.7\,$\mu$m,
    MRG~10\,$\mu$m, Sweeney~10\,$\mu$m, Sundt~1\,$\mu$m,
    Rattay~1\,$\mu$m; columns: two top-SI seeds chosen per row), giving
    ten realisations in total.  Each cell stacks a cross-section
    (top) and a per-contact pulse strip (bottom).  In the cross-section,
    the thick black line is the target / off-target divider (randomised
    per seed, uniform on $[0,\,180)$°); fibres are coloured by recruitment
    --- filled green = target fired, open green = target silent, red
    \texttimes{} = off-target fired (the leakage failure mode), small grey
    dot = off-target silent.  The six cuff contacts (sitting on a
    1.5\,mm ring around the nerve) are coloured by polarity (blue
    cathodic, orange anodic) and sized by $|I_k|$.  Each pulse strip
    underneath shows the optimised monophasic rectangular waveform for
    each of the six contacts (delay 1.0\,ms, pulse-width 0.1\,ms);
    the contact label C0--C5 and signed amplitude (mA) are printed
    inside the panel.  The selectivity index and seed number are noted
    in the top-right of each cross-section.  The optimised cuff
    configuration adapts to the randomised anatomy and recovers
    $\mathrm{SI}=1.0$ in the displayed cells; failure modes (red
    \texttimes{} markers) appear in the per-model distributions of
    Figure~\ref{fig:selectivity} but are absent from the
    top-SI subset shown here.  Failure-mode examples are shown in
    Figure~\ref{fig:selectivity-gallery-hard}.}
  \label{fig:selectivity-gallery}
\end{figure}

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_selectivity_xsections_hard.png}
  \caption{Companion to Figure~\ref{fig:selectivity-gallery}: the
    two \emph{worst-SI} seeds per fibre model, drawn with the same
    layout and marker scheme.  These are the residual failure cases
    --- the optimisation pipeline still reaches a stable local optimum
    on every seed, but the optimum has either some target fibres
    silent (open green markers) or some off-target fibres recruited
    (red \texttimes{} markers, the ``leakage'' mode), or both.  Visual
    inspection shows that the residual failures are typically driven
    by adversarial fascicle layouts where (i) a target and an
    off-target fascicle sit close together near the cuff perimeter,
    so any contact strong enough to drive the target also drives its
    neighbour, or (ii) a target fascicle lands near the geometric
    centre of the nerve where the field of any single ring contact is
    weak.  Anatomically realistic FEM-derived $V_e$ fields, which
    break the homogeneous-bath symmetry, are the natural next step
    for resolving these hard-anatomy cases.}
  \label{fig:selectivity-gallery-hard}
\end{figure}

\subsubsection*{Mixed-diameter regime.}
A second sweep targeted the harder problem of \emph{type
selectivity}---preferentially activating small ($D = 5.7\,\mu\mathrm{m}$)
target fibers without recruiting co-located large
($D = 14.0\,\mu\mathrm{m}$) off-target fibers---under the rationale
that the differential chronaxie between the two populations should be
exploitable by waveform shaping.  Across all attempted regularisation
strengths, soft-proxy temperatures, and Adam learning rates, the
optimiser invariably converged to the all-fibers-fire equilibrium
($\mathrm{SI} = 0$ for both rectangular and waveform stages; see
Section~\ref{sec:discussion} for analysis of why the loss surface
favours this attractor under spatially homogeneous extracellular
forcing in our six-contact cuff geometry).  We report this negative
result rather than omit it: it delineates the regime in which the
present cuff geometry and loss formulation are sufficient
(single-diameter spatial selectivity) versus the regime in which an
additional design lever---most plausibly anatomically realistic
$V_e$ with target-fascicle-adjacent contacts---is required.

To make the failure mode concrete, Figure~\ref{fig:mixed-landscape}
shows the loss landscape along a one-dimensional cut through amplitude
space for a representative mixed-diameter nerve.  We fix a bipolar
template ($I_0 = -1\,\mathrm{mA}$ cathodic at the contact closest to
the target fascicle, $I_3 = +1\,\mathrm{mA}$ anodic on the opposite
side, other contacts at zero) and sweep a single scaling factor
$\alpha$ from $0$ to $4\,\mathrm{mA}$.  Because the off-target
fascicle's $D = 14\,\mu\mathrm{m}$ fibres have a lower activation
threshold than the target fascicle's $D = 5.7\,\mu\mathrm{m}$ fibres
under the same pulse width, they fire first as $\alpha$ grows
(Figure~\ref{fig:mixed-landscape}a); over the entire intermediate
$\alpha$ regime the selectivity index is \emph{negative}
(Figure~\ref{fig:mixed-landscape}b, minimum
$\mathrm{SI} = -0.56$ at $\alpha \approx 0.7\,\mathrm{mA}$).  The
regularised WQ loss (Figure~\ref{fig:mixed-landscape}c) has two
attractive minima --- the trivial $\alpha = 0$ (silent) and the
saturated $\alpha \gg 0$ (all-fire) --- but no interior minimum at any
regularisation strength $\lambda$ we tested.  The differentiable
forward solver is gradient-informative at every $\alpha$; the
optimisation failure is a loss-landscape property, not a
gradient-availability one.

\begin{figure}[h]
  \centering
  \figmaybe[width=0.98\textwidth]{fig_mixed_diameter_landscape.png}
  \caption{Mixed-diameter loss-landscape diagnostic for one
    representative two-fascicle nerve
    ($D_{\mathrm{target}} = 5.7\,\mu\mathrm{m}$,
     $D_{\mathrm{off}} = 14\,\mu\mathrm{m}$, 25 fibres per fascicle).
    A bipolar amplitude template is fixed and scaled by a single
    factor $\alpha$, sweeping the entire range from silent
    ($\alpha = 0$) through threshold to saturation
    ($\alpha = 4\,\mathrm{mA}$).
    \textbf{(a)} Activation rates: the larger off-target fibres
    (red, $D = 14\,\mu\mathrm{m}$) cross threshold at lower
    $\alpha$ than the smaller target fibres (green,
    $D = 5.7\,\mu\mathrm{m}$), reflecting their lower
    pulse-width-equivalent threshold under the common monophasic
    rectangular pulse.
    \textbf{(b)} Selectivity index $\mathrm{SI}(\alpha)$ along the
    same sweep: across the entire intermediate $\alpha$ window the
    off-target population fires more strongly than the target, giving
    \emph{anti-selectivity} (SI $< 0$, shaded red); SI returns to
    zero at $\alpha = 0$ (no fibres fire) and at high $\alpha$ (all
    fibres fire).  There is no $\alpha$ along this slice that yields
    $\mathrm{SI} > 0$.
    \textbf{(c)} Regularised WQ loss
    $L(\alpha) = \mathrm{WQ}(\alpha) + \lambda \overline{I_k^{\,2}}$
    at four energy-regularisation strengths $\lambda$.  For
    $\lambda = 0$ the loss has two minima --- the silent $\alpha = 0$
    and a saturated large-$\alpha$ minimum where all fibres fire.
    Increasing $\lambda$ kills the large-$\alpha$ minimum but cannot
    create an interior selective one.  The optimiser, run from any
    initial $\alpha$, therefore lands at $\alpha = 0$ (trivial) or
    $\alpha$-large (all-fire), never at a positive-SI configuration.}
  \label{fig:mixed-landscape}
\end{figure}
